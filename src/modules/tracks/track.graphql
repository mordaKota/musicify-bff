type Track {
    id: ID!
    title: String!
    album: Album
    artists: [Artist]
    bands: [Band]
    duration: Int
    released: Int
    genres: [Genre]
}

input TrackInput {
    title: String!
    album: ID
    artists: [ID!]
    bands: [ID!]
    duration: Int
    released: Int
    genres: [ID!]
}
