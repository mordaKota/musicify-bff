type User {
    id: ID!
    firstName: String
    secondName: String
    password: String
    email: String!
}

input UserInput {
    firstName: String!
    lastName: String!
    password: String!
    email: String!
}

input Credentials {
    email: String!
    password: String!
}

type Jwt {
    jwt: String!
}
